//! Miscellaneous helper utilities.

/// Initialize logging/tracing system (placeholder).
pub fn init_logging() {
    // In a later task we'll wire `tracing_subscriber` here.
    todo!("Set up tracing subscriber");
}
