//! Command-line interface definition.

/// Parse command-line arguments and return a config override (stub).
///
/// In a future task we will integrate `clap` or a similar crate.
#[allow(dead_code)]
pub fn parse_args() {
    todo!("Implement CLI parsing");
}
